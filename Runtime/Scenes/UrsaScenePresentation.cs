namespace Ursa
{
    /// <summary>
    /// シーンを前面に出した時の表示方法。
    /// </summary>
    public enum UrsaScenePresentation
    {
        /// <summary>背面のシーンを隠す。</summary>
        Fullscreen,

        /// <summary>背面のシーンを表示したまま重ねる。</summary>
        Overlay
    }
}
